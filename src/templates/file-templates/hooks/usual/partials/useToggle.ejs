export function useToggle(initial<% if (useTypescript) { %>: boolean<% } %> = false)<% if (useTypescript) { %>: [boolean, () => void]<% } %> {
    const [state, setState] = useState(initial);
    const toggle = () => setState((prev) => !prev);

    return [state, toggle];
}